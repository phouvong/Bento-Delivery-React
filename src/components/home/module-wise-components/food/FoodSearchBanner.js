import ModuleSearchBanner from "../shared/ModuleSearchBanner";

const FoodSearchBanner = (props) => (
  <ModuleSearchBanner
    title="Search Delicious Food"
    subtitle="Easy search to easy order & get fast delivery for your need."
    {...props}
  />
);

export default FoodSearchBanner;
