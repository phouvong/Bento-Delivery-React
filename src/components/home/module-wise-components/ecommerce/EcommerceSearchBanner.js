import ModuleSearchBanner from "../shared/ModuleSearchBanner";

const EcommerceSearchBanner = (props) => (
  <ModuleSearchBanner
    title="Shop Your Favorites Now"
    subtitle="Browse thousands of products and get them delivered today."
    {...props}
  />
);

export default EcommerceSearchBanner;
