import ModuleSearchBanner from "../shared/ModuleSearchBanner";

const GrocerySearchBanner = (props) => (
  <ModuleSearchBanner
    title="Search Fresh Essentials Now"
    subtitle="Quickly find fresh items and get them delivered to your door."
    {...props}
  />
);

export default GrocerySearchBanner;
